#Based on the application name, we will fetch configs from git-local-config-repo
spring.application.name=limits-service
#Tell the limits-service where to look for configuration file , in our case at localhost:8888
spring.cloud.config.uri=http://localhost:8888
#set profile (default.dev,qa), based on it configuration will be fetched
spring.profiles.active=dev